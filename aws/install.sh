#!/usr/bin/env bash

set -o pipefail
set -o nounset
set -o errexit


echo "nothing for now since we use the docker image now"

#if which pyenv > /dev/null
#then
#  source "$DOTFILES/python/script_source.sh"

#  if pyenv virtualenvs | grep general3 > /dev/null
#  then
#    # assume that if pyenv is installed, 
#    # then the `general*` virtualenvs are set up as well
#    pyenv activate general3
#    pip install --upgrade awscli
#  fi

#  if pyenv virtualenvs | grep general2 > /dev/null
#  then
#    pyenv activate general2
#    pip install --upgrade awscli
#  fi
#fi
